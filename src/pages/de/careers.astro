---
import Layout from '../../layouts/Layout.astro';
import { getTranslation } from '../../lib/i18n';

// Helper function to get translation with dynamic values
const t = (key: string, values: Record<string, string> = {}) => {
  let translation = getTranslation(Astro, key);

  // Replace dynamic values in translations
  Object.entries(values).forEach(([placeholder, value]) => {
    translation = translation.replace(`{${placeholder}}`, value);
  });

  return translation;
};
---

<Layout title={t('careers.title')} description={t('careers.description')}>
  <!-- Hero Section -->
  <section class="py-20 lg:py-32 bg-gradient-to-br from-primary/10 via-secondary/5 to-accent/10 theme-transition">
    <div class="container mx-auto px-4">
      <div class="max-w-4xl mx-auto text-center">
        <div class="badge badge-outline badge-lg mb-6">
          <span class="flex items-center gap-2">
            <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M6 6V5a3 3 0 013-3h2a3 3 0 013 3v1h2a2 2 0 012 2v3.57A22.952 22.952 0 0110 13a22.95 22.95 0 01-8-1.43V8a2 2 0 012-2h2zm2-1a1 1 0 011-1h2a1 1 0 011 1v1H8V5zm1 5a1 1 0 011-1h.01a1 1 0 110 2H10a1 1 0 01-1-1z" clip-rule="evenodd"></path>
              <path d="M2 13.692V16a2 2 0 002 2h12a2 2 0 002-2v-2.308A24.974 24.974 0 0110 15c-2.796 0-5.487-.46-8-1.308z"></path>
            </svg>
            {t('careers.joinOurTeam')}
          </span>
        </div>
        <h1 class="text-4xl md:text-6xl font-bold mb-6">
          <span class="bg-gradient-to-r from-primary to-secondary bg-clip-text text-transparent">
            {t('careers.title')}
          </span>
        </h1>
        <p class="text-xl text-base-content/70 leading-relaxed mb-8">
          {t('careers.subtitle')}
        </p>
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
          <a href="#open-positions" class="btn btn-primary btn-lg">
            {t('careers.viewPositions')}
            <svg class="w-5 h-5 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3"></path>
            </svg>
          </a>
          <a href="#culture" class="btn btn-outline btn-lg">
            {t('careers.learnMore')}
          </a>
        </div>
      </div>
    </div>
  </section>

  <!-- Why Work With Us -->
  <section id="culture" class="py-20 lg:py-32 bg-base-100 theme-transition">
    <div class="container mx-auto px-4">
      <div class="max-w-6xl mx-auto">
        <div class="text-center mb-16">
          <h2 class="text-3xl md:text-5xl font-bold mb-4">
            <span class="bg-gradient-to-r from-primary to-secondary bg-clip-text text-transparent">
              {t('careers.whyWorkHere')}
            </span>
          </h2>
          <p class="text-lg text-base-content/70 leading-relaxed">
            {t('careers.whyWorkHereDesc')}
          </p>
        </div>

        <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
          <div class="card bg-base-200/50 shadow-xl">
            <div class="card-body p-8">
              <div class="w-12 h-12 bg-primary/10 rounded-lg flex items-center justify-center mb-4">
                <svg class="w-6 h-6 text-primary" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z"></path>
                </svg>
              </div>
              <h3 class="text-xl font-bold mb-2">{t('careers.benefits.health')}</h3>
              <p class="text-base-content/70">{t('careers.benefits.healthDesc')}</p>
            </div>
          </div>

          <div class="card bg-base-200/50 shadow-xl">
            <div class="card-body p-8">
              <div class="w-12 h-12 bg-secondary/10 rounded-lg flex items-center justify-center mb-4">
                <svg class="w-6 h-6 text-secondary" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253"></path>
                </svg>
              </div>
              <h3 class="text-xl font-bold mb-2">{t('careers.benefits.growth')}</h3>
              <p class="text-base-content/70">{t('careers.benefits.growthDesc')}</p>
            </div>
          </div>

          <div class="card bg-base-200/50 shadow-xl">
            <div class="card-body p-8">
              <div class="w-12 h-12 bg-accent/10 rounded-lg flex items-center justify-center mb-4">
                <svg class="w-6 h-6 text-accent" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z"></path>
                </svg>
              </div>
              <h3 class="text-xl font-bold mb-2">{t('careers.benefits.team')}</h3>
              <p class="text-base-content/70">{t('careers.benefits.teamDesc')}</p>
            </div>
          </div>

          <div class="card bg-base-200/50 shadow-xl">
            <div class="card-body p-8">
              <div class="w-12 h-12 bg-primary/10 rounded-lg flex items-center justify-center mb-4">
                <svg class="w-6 h-6 text-primary" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z"></path>
                </svg>
              </div>
              <h3 class="text-xl font-bold mb-2">{t('careers.benefits.innovation')}</h3>
              <p class="text-base-content/70">{t('careers.benefits.innovationDesc')}</p>
            </div>
          </div>

          <div class="card bg-base-200/50 shadow-xl">
            <div class="card-body p-8">
              <div class="w-12 h-12 bg-secondary/10 rounded-lg flex items-center justify-center mb-4">
                <svg class="w-6 h-6 text-secondary" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                </svg>
              </div>
              <h3 class="text-xl font-bold mb-2">{t('careers.benefits.flexibility')}</h3>
              <p class="text-base-content/70">{t('careers.benefits.flexibilityDesc')}</p>
            </div>
          </div>

          <div class="card bg-base-200/50 shadow-xl">
            <div class="card-body p-8">
              <div class="w-12 h-12 bg-accent/10 rounded-lg flex items-center justify-center mb-4">
                <svg class="w-6 h-6 text-accent" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                </svg>
              </div>
              <h3 class="text-xl font-bold mb-2">{t('careers.benefits.competitive')}</h3>
              <p class="text-base-content/70">{t('careers.benefits.competitiveDesc')}</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Open Positions -->
  <section id="open-positions" class="py-20 lg:py-32 bg-base-200/30 theme-transition">
    <div class="container mx-auto px-4">
      <div class="max-w-4xl mx-auto">
        <div class="text-center mb-16">
          <h2 class="text-3xl md:text-5xl font-bold mb-4">
            <span class="bg-gradient-to-r from-primary to-secondary bg-clip-text text-transparent">
              {t('careers.openPositions')}
            </span>
          </h2>
          <p class="text-lg text-base-content/70 leading-relaxed">
            {t('careers.positionsDesc')}
          </p>
        </div>

        <div class="space-y-6">
          <!-- Position 1 -->
          <div class="card bg-base-100 shadow-lg hover:shadow-xl transition-all duration-300">
            <div class="card-body p-8">
              <div class="flex flex-col lg:flex-row lg:justify-between lg:items-start gap-6">
                <div class="flex-1">
                  <h3 class="text-2xl font-bold mb-2">{t('careers.positions.seniorYogaInstructor.title')}</h3>
                  <div class="flex flex-wrap gap-3 mb-4">
                    <span class="badge badge-primary">{t('careers.positions.seniorYogaInstructor.type')}</span>
                    <span class="badge badge-outline">{t('careers.positions.seniorYogaInstructor.location')}</span>
                    <span class="badge badge-secondary">{t('careers.positions.seniorYogaInstructor.experience')}</span>
                  </div>
                  <p class="text-base-content/70 mb-4">
                    {t('careers.positions.seniorYogaInstructor.description')}
                  </p>
                  <div class="text-sm">
                    <span class="font-medium">{t('careers.positions.salary')}:</span>
                    <span class="text-success font-semibold ml-2">{t('careers.positions.seniorYogaInstructor.salary')}</span>
                  </div>
                </div>
                <button class="btn btn-primary lg:w-auto w-full">
                  {t('careers.applyNow')}
                </button>
              </div>
            </div>
          </div>

          <!-- Position 2 -->
          <div class="card bg-base-100 shadow-lg hover:shadow-xl transition-all duration-300">
            <div class="card-body p-8">
              <div class="flex flex-col lg:flex-row lg:justify-between lg:items-start gap-6">
                <div class="flex-1">
                  <h3 class="text-2xl font-bold mb-2">{t('careers.positions.fullstackDeveloper.title')}</h3>
                  <div class="flex flex-wrap gap-3 mb-4">
                    <span class="badge badge-primary">{t('careers.positions.fullstackDeveloper.type')}</span>
                    <span class="badge badge-outline">{t('careers.positions.fullstackDeveloper.location')}</span>
                    <span class="badge badge-secondary">{t('careers.positions.fullstackDeveloper.experience')}</span>
                  </div>
                  <p class="text-base-content/70 mb-4">
                    {t('careers.positions.fullstackDeveloper.description')}
                  </p>
                  <div class="text-sm">
                    <span class="font-medium">{t('careers.positions.salary')}:</span>
                    <span class="text-success font-semibold ml-2">{t('careers.positions.fullstackDeveloper.salary')}</span>
                  </div>
                </div>
                <button class="btn btn-primary lg:w-auto w-full">
                  {t('careers.applyNow')}
                </button>
              </div>
            </div>
          </div>

          <!-- Position 3 -->
          <div class="card bg-base-100 shadow-lg hover:shadow-xl transition-all duration-300">
            <div class="card-body p-8">
              <div class="flex flex-col lg:flex-row lg:justify-between lg:items-start gap-6">
                <div class="flex-1">
                  <h3 class="text-2xl font-bold mb-2">{t('careers.positions.marketingManager.title')}</h3>
                  <div class="flex flex-wrap gap-3 mb-4">
                    <span class="badge badge-primary">{t('careers.positions.marketingManager.type')}</span>
                    <span class="badge badge-outline">{t('careers.positions.marketingManager.location')}</span>
                    <span class="badge badge-secondary">{t('careers.positions.marketingManager.experience')}</span>
                  </div>
                  <p class="text-base-content/70 mb-4">
                    {t('careers.positions.marketingManager.description')}
                  </p>
                  <div class="text-sm">
                    <span class="font-medium">{t('careers.positions.salary')}:</span>
                    <span class="text-success font-semibold ml-2">{t('careers.positions.marketingManager.salary')}</span>
                  </div>
                </div>
                <button class="btn btn-primary lg:w-auto w-full">
                  {t('careers.applyNow')}
                </button>
              </div>
            </div>
          </div>
        </div>

        <div class="text-center mt-12">
          <p class="text-lg text-base-content/70 mb-6">
            {t('careers.noPositions')}
          </p>
          <button class="btn btn-outline btn-lg">
            {t('careers.spontaneousApplication')}
          </button>
        </div>
      </div>
    </div>
  </section>

  <!-- Application Process -->
  <section class="py-20 lg:py-32 bg-base-100 theme-transition">
    <div class="container mx-auto px-4">
      <div class="max-w-4xl mx-auto">
        <div class="text-center mb-16">
          <h2 class="text-3xl md:text-5xl font-bold mb-4">
            <span class="bg-gradient-to-r from-primary to-secondary bg-clip-text text-transparent">
              {t('careers.applicationProcess')}
            </span>
          </h2>
          <p class="text-lg text-base-content/70 leading-relaxed">
            {t('careers.processDesc')}
          </p>
        </div>

        <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-6">
          <div class="text-center">
            <div class="w-16 h-16 bg-primary text-primary-content rounded-full flex items-center justify-center mx-auto mb-4 text-2xl font-bold">
              1
            </div>
            <h3 class="text-xl font-bold mb-2">{t('careers.process.apply.title')}</h3>
            <p class="text-base-content/70">{t('careers.process.apply.description')}</p>
          </div>

          <div class="text-center">
            <div class="w-16 h-16 bg-secondary text-secondary-content rounded-full flex items-center justify-center mx-auto mb-4 text-2xl font-bold">
              2
            </div>
            <h3 class="text-xl font-bold mb-2">{t('careers.process.review.title')}</h3>
            <p class="text-base-content/70">{t('careers.process.review.description')}</p>
          </div>

          <div class="text-center">
            <div class="w-16 h-16 bg-accent text-accent-content rounded-full flex items-center justify-center mx-auto mb-4 text-2xl font-bold">
              3
            </div>
            <h3 class="text-xl font-bold mb-2">{t('careers.process.interview.title')}</h3>
            <p class="text-base-content/70">{t('careers.process.interview.description')}</p>
          </div>

          <div class="text-center">
            <div class="w-16 h-16 bg-success text-success-content rounded-full flex items-center justify-center mx-auto mb-4 text-2xl font-bold">
              4
            </div>
            <h3 class="text-xl font-bold mb-2">{t('careers.process.offer.title')}</h3>
            <p class="text-base-content/70">{t('careers.process.offer.description')}</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section class="py-20 lg:py-32 bg-gradient-to-br from-primary/10 via-secondary/5 to-accent/10 theme-transition">
    <div class="container mx-auto px-4">
      <div class="max-w-4xl mx-auto text-center">
        <h2 class="text-3xl md:text-5xl font-bold mb-6">
          <span class="bg-gradient-to-r from-primary to-secondary bg-clip-text text-transparent">
            {t('careers.readyToJoin')}
          </span>
        </h2>
        <p class="text-lg text-base-content/70 leading-relaxed mb-8">
          {t('careers.readyToJoinDesc')}
        </p>
        <button class="btn btn-primary btn-lg">
          {t('careers.applyNow')}
          <svg class="w-5 h-5 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"></path>
          </svg>
        </button>
      </div>
    </div>
  </section>
</Layout>

<script>
  // Smooth scroll for anchor links
  document.querySelectorAll('a[href^="#"]').forEach(anchor => {
    anchor.addEventListener('click', function (this: HTMLAnchorElement, e) {
      e.preventDefault();
      const href = this.getAttribute('href');
      if (!href) return;
      const target = document.querySelector(href);
      if (target) {
        target.scrollIntoView({
          behavior: 'smooth',
          block: 'start'
        });
      }
    });
  });
</script>